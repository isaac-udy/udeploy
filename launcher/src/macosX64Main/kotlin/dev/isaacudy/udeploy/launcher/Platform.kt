package dev.isaacudy.udeploy.launcher

actual fun runLauncher(args: Array<String>) {
    // macOS x64 webview integration not yet wired; arm64 has it.
    println("udeploy launcher (macos-x64) — UI not yet implemented for this target")
}
